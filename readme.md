# SCREEN_SAVER

This is a screensaver application developed in C# using Windows Forms. It provides a customizable screensaver for Windows systems.

## Build Command

To build and publish the application as a single self-contained executable:

```
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```